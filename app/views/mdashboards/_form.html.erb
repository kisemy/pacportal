<%= form_with(model: mdashboard, local: true) do |form| %>
  <% if mdashboard.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mdashboard.errors.count, "error") %> prohibited this mdashboard from being saved:</h2>

      <ul>
        <% mdashboard.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
